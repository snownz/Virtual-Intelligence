using System;
using System.Collections.Generic;
using System.Text;
using VI.Neural.ANNOperations;
using VI.NumSharp.Array;

namespace VI.Neural.Training
{
    public class BackPropagationTraining
    {
        public float TrainWithDesired(Array<float> desired)
        {
            return 0f;
        }

        public float TrainWithErrorPack(Array<float> error)
        {
            return 0f;
        }

        public float TrainWithoutValue()
        {
            return 0f;
        }
    }
}
